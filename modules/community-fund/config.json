{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "fundThreshold": {
      "type": "number",
      "default": 1000000,
      "minimum": 1,
      "description": "Target amount that triggers fund completion and reset"
    },
    "minimumContribution": {
      "type": "number",
      "default": 1,
      "minimum": 1,
      "description": "Minimum amount a player must contribute per command"
    },
    "completionMessage": {
      "type": "string",
      "default": "The community fund has reached {threshold}! Thank you everyone!",
      "description": "Broadcast message when threshold is reached. Supports {threshold} placeholder."
    },
    "completionCommands": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "description": "Raw game server commands executed on fund completion. E.g. Minecraft: 'effect give @a luck 3600'"
    },
    "broadcastContributions": {
      "type": "boolean",
      "default": true,
      "description": "Whether to broadcast each contribution to all players"
    }
  },
  "required": [],
  "additionalProperties": false
}
