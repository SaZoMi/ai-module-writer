import { data, takaro, TakaroUserError, checkPermission } from '@takaro/helpers';

async function main() {
  // v1: async puzzle sessions persist on disconnect; live rounds continue
  // No cleanup needed
}

await main();
